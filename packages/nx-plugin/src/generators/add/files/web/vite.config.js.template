import { defineConfig } from 'vite';

export default defineConfig({
  root: __dirname,
  cacheDir: '../../node_modules/.vite/apps/<%= name %>',
  server: {
    host: '0.0.0.0',
    port: 8080,
  },
});
